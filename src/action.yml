name: 'Autograder'
description: 'Autograde a repo submission and return results'
outputs:
  token: # id of output
    description: 'ID token generated from OIDC'
runs:
  using: 'node16'
  main: 'index.js'
